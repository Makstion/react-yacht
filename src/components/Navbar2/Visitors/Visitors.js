import React from "react";

const Visitors = (props) => {
  // let visitorElements = props.visitorData.map((v) => <div>{v.name}</div>);

  return (
    <div className="block">
      <div className="block-header">Visitors</div>
      {/* <div className="block-body visitors">{visitorElements}</div> */}
    </div>
  );
};

export default Visitors;
